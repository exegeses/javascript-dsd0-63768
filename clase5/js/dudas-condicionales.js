// el = (uno sólo) es el operador de ASIGNACIÖN
let nombre = 'Marcos';


/* en la paréntesis del if
    si queremos COMPARAR el valos
    de una variable, debemos usar 
    el == ( doble ) es el operador de COMPARACIÓN
*/
if( nombre == 'Admin' ){
    document.write('bienvenido Admin');
}